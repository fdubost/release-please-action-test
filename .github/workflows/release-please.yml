on:
  push:
    branches:
      - master
name: release-please
jobs:
  release-please:
    runs-on:
      - self-hosted
      - yarn-medium
    steps:
      - uses: google-github-actions/release-please-action@v3
        with:
          release-type: node
          package-name: release-please-action-test
